<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rudysorto.inventario">


    <!-- para guardar chart como imagen  -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <!-- To access Google+ APIs: -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!--
 To retrieve OAuth 2.0 tokens or invalidate tokens to disconnect a user. This disconnect
     option is required to comply with the Google+ Sign-In developer policies
    -->
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />
    <!-- To retrieve the account name (email) as part of sign-in: -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <!--
     The following two permissions are not required to use
     Google Maps Android API v2, but are recommended.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />


    <permission
        android:name="com.rudysorto.mapsv2.permission.MAPS_RECEIVE"
        android:protectionLevel="signature" />

    <uses-permission android:name="com.rudysorto.mapsv2.permission.MAPS_RECEIVE" />
    <!--   android:theme="@style/AppTheme" >  android:theme="@android:style/Theme.Holo.Light">  android:icon="@mipmap/ic_launcher" -->
    <application
        android:name="com.rudysorto.inventario.GlobalVar"
        android:allowBackup="true"

    android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Holo.Light">

    <activity
            android:name=".Login"
            android:label="@string/app_name">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity
            android:name=".appFuerzaVentas.MuestraMedicaActivity"
            android:label="@string/app_name">

        </activity>
        <activity
            android:name=".PruebaCustonAutoCom"
            android:label="@string/app_name">

        </activity>
        <activity
        android:name=".pdfActivity"
        android:label="@string/app_name">

    </activity>
        <activity
            android:name=".appFuerzaVentas.Graficabarra1"
            android:label="@string/app_name">

        </activity>
        <activity
            android:name=".Grafico"
            android:label="@string/app_name">

        </activity>

        <activity
            android:name=".appFuerzaVentas.SupervisarMuestraMedicaActivity"
            android:label="@string/app_name">

        </activity>

        <activity
            android:name=".GraficaAcitivity"
            android:label="@string/app_name">

        </activity>

        <activity
            android:name=".appFuerzaVentas.BodegaVirtualActivity"
            android:label="@string/app_name">

        </activity>

        <activity
            android:name=".MenuPrincipalOpciones"
            android:label="@string/app_name">

        </activity>

        <activity
            android:name=".appFuerzaVentas.BitacoraActivity"
            android:label="@string/app_name">
            <!--  <intent-filter>
            <action android:name="android.intent.action.MAIN" />

               <category android:name="android.intent.category.LAUNCHER" />
           </intent-filter>-->
        </activity>

        <activity
            android:name=".MenuPrincipal"
            android:label="@string/title_activity_menu_principal">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        <activity
            android:name=".Splash"
            android:label="@string/title_activity_splash">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".appFuerzaVentas.ActivityAppFuerzaVentas"
            android:label="@string/title_activity_activity_app_fuerza_ventas"
            android:theme="@style/AppTheme.NoActionBar"></activity>

        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyBELrI89UWNaSjoMjdQvUnLbZ0t1vlwU24"
            />
       <!--     android:value="AIzaSyAvgXR2DJx4VvIduEUlyPq9aXOKYw19KPc" -->


    </application>

</manifest>
